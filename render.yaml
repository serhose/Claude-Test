services:
  - type: web
    name: cv-tailor
    runtime: python
    rootDir: cv-tailor
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false
